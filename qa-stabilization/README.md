# Stabilization QA Artifact Archive

This directory stores supporting artifacts from earlier stabilization passes. These files are retained for traceability, not as the current source of truth.

Current pass reports live in `docs/stabilization/`. When a new validation pass runs, update or add a report there rather than reusing older screenshots/logs as fresh evidence.
